
# 🤖 Telegram Bot для проверки лабораторных работ

Бот для автоматической проверки лабораторных работ студентов с использованием AI (OpenRouter). Поддерживает файлы PDF, DOCX, TXT.

---

## 📋 Содержание

- [Изменения в коде](#изменения-в-коде)
- [Установка](#установка)
- [Настройка](#настройка)
- [Использование](#использование)
- [Команды](#команды)
- [Требования](#требования)
- [Зависимости](#зависимости)
- [Решение проблем](#решение-проблем)

---

## 🔄 Изменения в коде

### 1. **Исправление модели OpenRouter**

**Было:**
```python
model="qwen/qwen3-235b-a22b:free"  # Конкретная модель
```

**Стало:**
```python
model="openrouter/free"  # Автоматический выбор лучшей бесплатной модели
```

*Причина:* Модель `qwen/qwen3-235b-a22b:free` больше не доступна, используется автоопределение

---

### 2. **Добавлена очистка Markdown-разметки**

**Было:** Ответы ИИ отображались с символами `**` (звездочками)

**Стало:** Добавлена функция `clean_text()` для удаления Markdown

```python
def clean_text(text: str) -> str:
    """Удаляет markdown-разметку"""
    # Заменяем ** на пустоту (убираем жирный)
    text = text.replace('**', '')
    # Заменяем * на пустоту (убираем курсив)
    text = text.replace('*', '')
    # Заменяем _ на пустоту
    text = text.replace('_', '')
    # Заменяем ` на пустоту (код)
    text = text.replace('`', '')
    return text
```

---

### 3. **Новая команда /models**

Добавлена команда для просмотра доступных бесплатных моделей:

| Модель | Описание |
|--------|----------|
| `openrouter/free` | Авто выбор лучшей |
| `deepseek/deepseek-r1:free` | Для сложных задач |
| `arcee-ai/trinity-large-preview:free` | Креативные задачи |
| `google/gemini-2.0-flash-exp:free` | Быстрая модель |
| `qwen/qwen-2.5-72b-instruct:free` | Мощная модель |
| `meta-llama/llama-3.3-70b-instruct:free` | Llama от Meta |

---

### 4. **Улучшена обработка длинных файлов**

**Было:** Обработка любых файлов без ограничений

**Стало:** Обрезка контента до 15000 символов

```python
# Обрезаем слишком длинный контент
if len(content) > 15000:
    content = content[:15000] + "...\n\n[Текст обрезан из-за длины]"
```

---

### 5. **Расширена главная клавиатура**

**Было:**
```python
def get_main_keyboard():
    return InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="❓ Помощь", callback_data="help")]
    ])
```

**Стало:**
```python
def get_main_keyboard():
    return InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="❓ Помощь", callback_data="help")],
        [InlineKeyboardButton(text="📊 Модели", callback_data="models")]
    ])
```

---

### 6. **Улучшен формат ответа ИИ**

**Было:** Свободный формат ответа

**Стало:** Структурированный формат с эмодзи

```
📊 КРАТКИЙ АНАЛИЗ:
[2-3 предложения о работе]

📈 ОЦЕНКА ПО КРИТЕРИЯМ:
• Качество кода: X/30 - [обоснование]
• Полнота реализации: X/30 - [обоснование]
• Документация: X/20 - [обоснование]
• Оформление: X/20 - [обоснование]

✅ СИЛЬНЫЕ СТОРОНЫ:
• [список]

💡 ЧТО УЛУЧШИТЬ:
• [конкретные рекомендации]

🎯 ИТОГОВАЯ ОЦЕНКА: X/100
```

---

### 7. **Улучшена обработка ошибок**

- ✅ Проверка существования временных файлов перед удалением
- ✅ Обрезка длинных сообщений об ошибках (до 200 символов)
- ✅ Более информативные сообщения пользователю

```python
except Exception as e:
    if temp_path and os.path.exists(temp_path):
        os.unlink(temp_path)
    error_text = str(e)
    await message.answer(f"❌ Ошибка: {error_text[:200]}")
```

---

### 8. **Изменен интерфейс команд**

**Было в `/start`:**
```
Просто отправь мне файл с работой!
```

**Стало:**
```
1. Отправь мне файл с работой
2. Подожди проверку (1-2 минуты)
3. Получи оценку и рекомендации
```

---

### 9. **Добавлен подсчет символов**

Бот показывает количество символов в обработанном файле:

```
📝 Символов: 5,573
```

---

### 10. **Улучшен вывод в консоль**

**Было:**
```
🤖 Запускаю бот для проверки лабораторных работ...
```

**Стало:**
```
==================================================
🤖 Telegram бот для проверки лабораторных работ
==================================================
📁 Поддерживаемые форматы: .pdf, .docx, .txt
📊 Макс. размер файла: 20.0 МБ
🤖 Модель: openrouter/free (автовыбор лучшей)
⚡ Бот запущен! Отправьте файл для проверки
==================================================
🛑 Для остановки нажмите Ctrl+C
==================================================
```

---

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <url-репозитория>
cd telegram_lab_bot
```

### 2. Создайте виртуальное окружение

**Для Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Для Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

---

## 🔧 Настройка

### 1. Создайте файл `.env`

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENROUTER_API_KEY=ваш_ключ_openrouter
```

### 2. Получите токены

- **Telegram Bot Token:** Напишите [@BotFather](https://t.me/botfather) в Telegram
- **OpenRouter API Key:** Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai)

---

## 🎮 Использование

### Запуск бота

```bash
python bot.py
```

### В Telegram

1. Найдите бота по его username
2. Отправьте команду `/start`
3. Загрузите файл с лабораторной работой (PDF, DOCX или TXT)
4. Получите результат проверки через 1-2 минуты

---

## 📚 Команды

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы с ботом |
| `/help` | Справка по использованию |
| `/models` | Список доступных AI моделей |

---

## 📁 Поддерживаемые форматы

| Формат | Макс. размер | Описание |
|--------|--------------|----------|
| PDF | 20 МБ | Документы PDF |
| DOCX | 20 МБ | Microsoft Word |
| TXT | 20 МБ | Текстовые файлы |

---

## 🔧 Требования

- Python 3.8 или выше
- Telegram Bot Token
- OpenRouter API Key
- Интернет-соединение

---

## 📦 Зависимости

```txt
aiogram==3.4.1          # Telegram Bot API
openai==1.12.0          # OpenRouter клиент
python-dotenv==1.0.0    # Работа с .env файлами
python-docx==1.1.0      # Чтение DOCX файлов
PyPDF2==3.0.1           # Чтение PDF файлов
aiofiles==23.2.1        # Асинхронная работа с файлами
```

---

## ⚠️ Примечания

- ✅ Бот использует **бесплатные модели** OpenRouter (с суффиксом `:free`)
- ✅ Длинные файлы обрезаются до **15000 символов**
- ✅ Результаты проверки разбиваются на части (лимит Telegram **4096 символов**)
- ✅ При возникновении ошибок проверьте токены в `.env` файле

---

## 🆘 Решение проблем

### Проблема: Бот показывает звездочки (**) вместо текста

**Решение:** Используется функция `clean_text()` для очистки Markdown

```python
# Автоматически очищает ответ от **, *, _, `
clean_result = clean_text(result)
```

---

### Проблема: Модель не найдена (404)

**Решение:** Используется `openrouter/free` для автовыбора

```
Ошибка 404 → автоматическое переключение на openrouter/free
```

---

### Проблема: Файл не читается

**Решение:** Проверьте:

1. Формат файла (поддерживаются только PDF, DOCX, TXT)
2. Размер файла (макс. 20 МБ)
3. Содержимое (файл не должен быть пустым)
4. Кодировку (для TXT используйте UTF-8)

---

### Проблема: Бот не отвечает

**Решение:**

```bash
# Проверьте, запущен ли бот
ps aux | grep python

# Проверьте токены в .env
cat .env

# Перезапустите бот
python bot.py
```

---

## 📝 Лицензия

MIT License

---

## 👨‍💻 Автор

Ваше имя

---

## ⭐ Поддержка

Если проект полезен, поставьте звезду на GitHub!
```

Этот README.md полностью готов к использованию и содержит всю информацию о внесенных изменениях в правильном Markdown-формате с таблицами, код-блоками и эмодзи для лучшей читаемости.